apiVersion: v1
kind: Service

metadata:
  name: document-analyzer-service
  namespace: miniproject-management

spec:
  type: ClusterIP

  selector:
    app: document-analyzer

  ports:
    - port: 8080
      targetPort: 8080